# Copy this to docker/.env (DO NOT commit docker/.env).
# `docker/.env` is read by the `docker compose ... --env-file docker/.env` command in this repo.

# Container runtime settings
TZ=Africa/Cairo
APP_ENV=dev

# Build settings (optional)
# BASE_IMAGE must be Debian/Ubuntu-family (this Dockerfile uses `apt-get`).
BASE_IMAGE=ubuntu:24.04

# Optional Swift service base image tag (used only when enabling the `swift` profile).
SWIFT_IMAGE=swift:5.10-jammy

# User settings (optional)
# On Linux hosts, setting USER_UID/USER_GID to match your host user helps avoid permission issues
# when the repo is mounted into the container. (Get these values via `id -u` and `id -g`.)
USERNAME=dev
USER_UID=1000
USER_GID=1000
